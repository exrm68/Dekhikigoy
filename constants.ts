import { Movie } from './types';

// Categories
export const CATEGORIES = ['All', 'Exclusive', 'Korean Drama', 'Series', 'Action', 'Comedy', 'Horror', 'Romance', 'Favorites'];

// Default Bot Username (can be changed from admin panel)
export const BOT_USERNAME = 'your_bot';
